<UserControl x:Class="UI.View.TourDetailHandlerWindowComponents.ConfirmButtonControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.View.TourDetailHandlerWindowComponents"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="confirm" Grid.Column="1" Command="{Binding TourDetailCreationCommand}" Margin="5">Create</Button>
        <Button x:Name="back" Grid.Column="2" Command="{Binding QuitTourDetailCreationCommand}" Margin="5">Back</Button>
    </Grid>
</UserControl>
